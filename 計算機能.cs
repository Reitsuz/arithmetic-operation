/* style.css */
.calculator {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
    max-width: 300px;
    margin: 0 auto;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

button {
    font-size: 18px;
    padding: 10px;
    background-color: #f0f0f0;
    border: none;
    cursor: pointer;
}
